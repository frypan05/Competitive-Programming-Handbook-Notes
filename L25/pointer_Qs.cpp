//https://www.naukri.com/code360/guided-paths/pointers?nps=true


#include<bits/stdc++.h>
using namespace std;

int main() {

    /*
    What will be the output ?

    float f = 10.5;
    float p = 2.5;
    float* ptr = &f;
    (*ptr)++;
    *ptr = p;
    cout << *ptr << “ “ << f << “ “ << p;
    */

    /*What will be the output?
    int a = 7;
    int b = 17;
    int *c = &b;
    *c = 7;
    cout  << a << "  “ << b << endl;
    */

   /*
    int *ptr = 0;
    int a = 10;
    *ptr = a;
    cout << *ptr << endl;

    //The program is likely to crash with a segmentation fault or similar error when you run it. 
    //This happens because dereferencing a null pointer (*ptr) attempts to access a memory location 
    //that the program is not allowed to use.
   */

    /*What will be the output ?

    char ch = 'a';
    char* ptr = &ch;
    ch++;
    cout << *ptr << endl;
    */

    /*Problem statement
    int a = 7;
    int *c = &a;
    c = c + 1;
    cout  << a << "  " << *c << endl;
    
    */

    /*Problem statement
    Assume the memory address of variable ‘a’ is 400 
    (and an integer takes 4 bytes), what will be the output -

    int a = 7;
    int *c = &a;
    c = c + 3;
    cout<< c << endl;
    
    */


    /*Problem statement
    Assume memory address of variable ‘a’ is : 200 and a double variable 
    is of size 8 bytes, what will be the output -

    double a = 10.54;
    double *d = &a;
    d = d + 1;
    cout  << d << endl;
    
    */

   /*
   
   */




    
    return 0;
}